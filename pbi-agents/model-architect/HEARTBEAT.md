# Heartbeat Checklist — Model Architect

- Check for pending tasks from Supervisor via `sessions_list`.
- If a modeling task is assigned, load project WORKING.md.
- If nothing needs attention, reply HEARTBEAT_OK.
